# Environment variables for dsp_web_frontend (CRA)
# Copy to .env and adjust values as needed.

# Base URL for the DSP backend API.
# When empty or not set, the app will run in MOCK MODE and use local placeholder data.
REACT_APP_DSP_API_BASE_URL=
